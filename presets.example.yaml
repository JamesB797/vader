defaults:
  # These options apply to all projects
  options: ["--vim", "--max-chat-history-tokens", "10000"]

projects:
  vader:  # Presets for the Vader repository itself
    defaults:
      files: ["readme.md", "vader.py", "presets.example.yaml"]
